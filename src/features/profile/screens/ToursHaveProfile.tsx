interface Props {
  userId: number;
}

const ToursHaveProfile = ({ userId }: Props) => {
  return (
    <div className="w-full inline-flex overflow-x-auto beautiful-scrollbar">
      <div />
    </div>
  );
};

export default ToursHaveProfile;
